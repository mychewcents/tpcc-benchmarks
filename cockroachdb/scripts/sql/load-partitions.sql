INSERT INTO defaultdb.ORDERS_WID_DID (
  O_W_ID, 
  O_D_ID, 
  O_ID, 
  O_C_ID, 
  O_CARRIER_ID, 
  O_OL_CNT, 
  O_ALL_LOCAL, 
  O_ENTRY_D, 
  O_TOTAL_AMOUNT) 
SELECT 
  O_W_ID, 
  O_D_ID, 
  O_ID, 
  O_C_ID, 
  CASE WHEN O_CARRIER_ID IS NULL THEN 0 ELSE O_CARRIER_ID END, 
  O_OL_CNT, 
  O_ALL_LOCAL, 
  O_ENTRY_D, 
  0.0
FROM defaultdb.ORDERS WHERE O_W_ID = WID AND O_D_ID = DID;

INSERT INTO defaultdb.ORDER_LINE_WID_DID 
SELECT * FROM defaultdb.ORDER_LINE
WHERE OL_W_ID = WID AND OL_D_ID = DID;
